{
  "name": "functions",
  "private": true,
  "main": "lib/index.js",
  "type": "module",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "npm --prefix .. run build -w cah-game-engine && node esbuild.mjs",
    "typecheck": "tsc --noEmit",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "@types/node": "^22.19.19",
    "cah-game-engine": "file:../packages/game-engine",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
